---
title: VoLTE Mutation Fuzzer - 발전 로드맵
created: 2026-03-23
tags:
  - roadmap
  - planning
  - volte
  - fuzzing
  - future
aliases:
  - 로드맵
  - Roadmap
---

# VoLTE Mutation Fuzzer — 발전 로드맵

> [!abstract] 요약
> Phase 1~5(프로토콜 모델 → Generator → Mutator → Sender → Oracle/Campaign)까지 구현 완료. 캠페인 자동화와 3축 이상 탐지(소켓 + 프로세스 + 로그)가 동작한다. 다음 과제는 **stateful dialog fuzzing**, **실 단말(Android) 연동**, **P-CSCF 경유 전송**이다.

---

## 1. 현재 위치 진단

### 1.1 Phase 완료 현황

| Phase | 내용 | 상태 | 비고 |
|-------|------|:----:|------|
| Phase 1 | SIP 메시지 분류 + 카탈로그 | ✅ | 14 메서드 + 75 응답코드, IANA 전수조사 완료 |
| Phase 2 | Generator (베이스라인 생성) | ✅ | 전체 메서드/응답 지원, CLI 통합 |
| Phase 3 | Mutator (3계층 뮤테이션) | ✅ | model/wire/byte, 시드 기반 재현 |
| Phase 4 | Sender/Reactor (전송/수집) | ✅ | softphone + real-ue-direct 구현 |
| Phase 5 | Oracle + Campaign | ✅ | SocketOracle + ProcessOracle + LogOracle 3축 판정, Tier 기반 캠페인 자동화, JSONL 결과 저장, report/replay CLI |

### 1.2 현재 할 수 있는 것

```bash
# 단일 패킷 파이프라인
uv run fuzzer request INVITE \
  | uv run fuzzer mutate packet --strategy state_breaker --seed 42 \
  | uv run fuzzer send packet --target-host 10.20.20.2 --target-port 5060

# 캠페인 자동 실행
uv run fuzzer campaign run --target-host 10.20.20.2 --scope tier1 \
  --process-name baresip --log-path /var/log/baresip.log
```

- 14개 메서드, 75개 응답코드에 대한 정상 패킷 생성
- model/wire/byte 3계층 뮤테이션 적용
- softphone 또는 실 UE(direct)에 단일 패킷 전송
- 소켓 응답 수집 및 outcome 분류 (provisional/success/error/timeout)
- **3축 이상 판정**: 소켓 응답 + 프로세스 생존(pgrep) + 로그 패턴 매칭
- **캠페인 자동화**: Tier 기반 수백~수천 케이스 순차 실행
- **결과 저장/분석**: JSONL append-only 저장, report/replay CLI
- **verdict**: normal / suspicious / timeout / crash / stack_failure / unknown

### 1.3 현재 할 수 **없는** 것

| 영역 | 부재 내용 |
|------|----------|
| **Stateful dialog fuzzing** | INVITE→PRACK→BYE 시퀀스 내부 뮤테이션 불가 |
| **P-CSCF 경유** | real-ue-pcscf 인터페이스만 존재, 구현 없음 |
| **ADB/logcat 연동** | Android 실 단말 crash/ANR 탐지 미지원 (현재는 파일 기반 로그만) |
| **관측 플러그인** | siptrace, pcap 미연동 |
| **병렬 퍼징** | 동기 순차 실행만 가능 |

### 1.4 강점과 약점

> [!success] 강점
> - SIP 표면 인벤토리가 **전수 수준**으로 깊다
> - Generator → Mutator → Sender → Oracle → Campaign 전체 파이프라인 동작
> - 문서, 카탈로그, 구현이 **같은 축**을 보고 있다
> - "단말 지향 퍼징"이라는 문제 정의가 흔들리지 않는다
> - 시드 기반 결정적 재현 가능
> - 3축 Oracle(소켓 + 프로세스 + 로그)로 **다각도 이상 탐지**

> [!warning] 약점
> - **stateless 퍼징만 지원** — 다이얼로그 시퀀스 내부 변이 불가
> - 관측이 소켓 + 로컬 프로세스/로그에 한정 — Android 실 단말에서는 부족
> - 대상 단말(ISSUE-01) **미결정**
> - 소프트폰 실제 검증 미완

---

## 2. 미해결 의사결정

다음 단계로 넘어가기 전에 **반드시 결정해야 하는** 핵심 질문들이다.

### 2.1 ISSUE-01: 초기 타깃 단말 선정

| 선택지 | 장점 | 단점 |
|--------|------|------|
| **Android (특정 모델)** | ADB/logcat 접근 용이, 크래시 로그 풍부 | 제조사별 SIP 스택 차이 |
| **iOS** | VoLTE 사용률 높음 | 관측 채널 극히 제한 |
| **소프트폰 (Baresip/PJSIP)** | 완전 제어 가능, 로그 풍부 | 실 단말 대표성 부족 |

> [!question] 결정 필요
> 1차 실험 대상은 **소프트폰 sanity** → **Android 실 단말** 순서가 현실적.
> 구체적 모델·OS 버전은 실험 환경 확보 후 확정.

### 2.2 ISSUE-05: Oracle 관측 채널 선택

| 채널 | 감지 가능 | 구현 난이도 |
|------|----------|:----------:|
| **소켓 응답 분석** | timeout, 비정상 응답 코드, 응답 부재 | 낮음 (이미 구현) |
| **ADB logcat** | 크래시, ANR, 프로세스 재시작 | 중간 |
| **ADB bugreport** | 시스템 레벨 크래시 덤프 | 중간 |
| **네트워크 캡처 (tcpdump/pcap)** | 패킷 도달 여부, 중간 드롭 | 중간 |
| **디바이스 상태 폴링** | 재부팅, 화면 상태, 프로세스 생존 | 낮음 |

> [!question] 결정 필요
> 최소 구현은 **소켓 응답 + ADB logcat + 디바이스 상태 폴링** 조합이 적절.

### 2.3 기타 미결 사항

| 항목 | 현재 상태 | 필요한 결정 |
|------|----------|-----------|
| IMS 인증(AKA) 시뮬레이션 | 미구현 | REGISTER/인증 완료 상태를 전제할 것인가 |
| real-ue-pcscf 구현 시점 | 인터페이스만 존재 | direct 경로 검증 후 진행 여부 |
| TCP/TLS 전송 우선순위 | UDP 우선 결정 완료 | TCP/TLS는 언제 추가할 것인가 |
| 소프트폰 선택 | Baresip 런처 구현 | Linphone/PJSIP 통합 시점 |

---

## 3. Phase 5 구현 완료 사항

### Oracle — 이상 탐지 엔진 ✅

> [!info] 구현 완료
> 소켓 응답 + 프로세스 생존 + 로그 패턴 매칭 3축 판정. 상세는 [[구현-문서]] 7장 참고.

구현된 컴포넌트:

| 컴포넌트 | 역할 | 상태 |
|---------|------|:----:|
| `OracleVerdict` | 판정 결과 데이터 모델 | ✅ |
| `SocketOracle` | 소켓 응답 기반 판정 (5xx, 타임아웃, 파싱 실패) | ✅ |
| `ProcessOracle` | `pgrep` 프로세스 생존 확인 → crash 판정 | ✅ |
| `LogOracle` | 로그 파일 incremental read, 스택 트레이스 패턴 매칭 → stack_failure 판정 | ✅ |
| `OracleEngine` | 3축 조합 판정 (stack_failure > crash > socket verdict) | ✅ |

판정 결과: `normal` | `suspicious` | `timeout` | `crash` | `stack_failure` | `unknown`

> [!note] 설계 시점과의 차이
> - ADB Observer, PcapObserver는 미구현 — 소프트폰 우선 전략에 따라 파일 기반 LogOracle로 대체
> - ANR, REBOOT verdict는 Android 실 단말 연동 시 추가 예정

---

### Campaign — 캠페인 오케스트레이션 ✅

> [!info] 구현 완료
> Tier 기반 케이스 자동 생성, 순차 실행, JSONL 결과 저장, report/replay CLI. 상세는 [[구현-문서]] 8장 참고.

구현된 컴포넌트:

| 컴포넌트 | 역할 | 상태 |
|---------|------|:----:|
| `CampaignConfig` | 캠페인 설정 (대상, 범위, 전략, 시드, 로그 경로) | ✅ |
| `CaseGenerator` | Tier 기반 method × layer × strategy 조합 생성 | ✅ |
| `CampaignExecutor` | 순차 실행 루프 (생성→변이→전송→판정→저장) | ✅ |
| `ResultStore` | append-only JSONL 영속 저장 (crash-safe) | ✅ |
| `campaign report` | 결과 요약 + verdict 필터링 | ✅ |
| `campaign replay` | 시드 기반 특정 케이스 재현 | ✅ |

> [!note] 설계 시점과의 차이
> - 저장 형식은 JSON 단일 파일 대신 **JSONL** (append-only, crash-safe)
> - SQLite 전환은 보류 — 현재 규모에서는 JSONL + jq로 충분
> - 병렬 실행 미구현 — 동기 순차 실행만

---

## 4. 단기 로드맵

Phase 5 안정화 이후 우선 진행할 영역.

### 4.1 P-CSCF 경유 경로 (`real-ue-pcscf`)

| 항목 | 내용 |
|------|------|
| **목적** | IMS 네트워크 경유 전송으로 Tier 3 공격면(IMS private headers) 실험 |
| **전제** | Open5GS + Kamailio 환경에서 P-CSCF 라우팅 검증 완료 |
| **구현** | `SIPSenderReactor`에 pcscf 모드 추가, P-CSCF 주소 해석 |
| **가치** | `P-Asserted-Identity`, `P-Charging-*`, `Feature-Caps` 등 IMS 전용 표면 자극 |

### 4.2 다중 관측자 통합

```
Observer Plugin Architecture:
  ┌─────────────┐
  │ ObserverBase │  ← abstract
  ├─────────────┤
  │ SocketObs   │  ← SIP 응답 분석
  │ ADBObs      │  ← logcat/bugreport
  │ PcapObs     │  ← tcpdump/tshark
  │ DevicePoll  │  ← 생존/상태 확인
  │ SIPTraceObs │  ← Kamailio siptrace
  └─────────────┘
```

- 각 Observer는 독립 실행, 결과를 `OracleAggregator`에 전달
- 플러그인 구조로 환경별 Observer 조합 가능

### 4.3 IMS 인증 시뮬레이션

| 단계 | 내용 |
|------|------|
| 1단계 | REGISTER 완료 상태 가정 (현재) |
| 2단계 | Digest 인증 challenge-response 자동 처리 |
| 3단계 | IMS AKA (3GPP TS 33.203) 시뮬레이션 |

- Generator에 인증 컨텍스트 주입 기능 추가
- Mutator에 인증 필드 특화 뮤테이션 전략 추가

### 4.4 TCP/TLS 전송 지원

| 항목 | 현재 | 목표 |
|------|------|------|
| UDP | ✅ 구현 완료 | 유지 |
| TCP | ✅ Sender에 구현 | 캠페인 통합 |
| TLS | ❌ 미구현 | SIPS URI 지원 + TLS 소켓 |
| WebSocket | ❌ | RFC 7118 기반 WS/WSS |

### 4.5 소프트폰 확장

| 소프트폰 | 용도 | 통합 방식 |
|---------|------|----------|
| **Baresip** | CLI 자동화 | ✅ 런처 구현, 설정 기반 |
| **Linphone** | GUI 수동 검증 | 수동 실행, 로그 수집 |
| **PJSIP/PJSUA2** | 프로그래밍 자동화 | Python 바인딩, 콜백 기반 |

> [!tip] 추천 순서
> Baresip (현재) → PJSIP (자동화 강화) → Linphone (수동 보조)

---

## 5. 장기 로드맵

### 5.1 스마트 뮤테이션 (ML/Feedback-Driven)

| 접근 | 설명 |
|------|------|
| **Coverage-Guided** | 코드 커버리지 피드백으로 뮤테이션 방향 조정 (requires instrumentation) |
| **Grammar-Aware** | SIP 문법 트리 기반 구조적 뮤테이션 생성 |
| **Reinforcement Learning** | 과거 crash 발견 패턴에서 다음 뮤테이션 전략 학습 |
| **Corpus Distillation** | 유효한 뮤테이션 집합을 축소하여 효율 극대화 |

> [!note] 현실적 접근
> 블랙박스 퍼징이므로 coverage-guided는 어렵다. **Grammar-aware + corpus distillation**이 현실적 첫 단계.

### 5.2 병렬/분산 퍼징

```
Campaign Controller
  ├── Worker 1 (Android UE #1, Tier 1 mutations)
  ├── Worker 2 (Android UE #2, Tier 2 mutations)
  ├── Worker 3 (Softphone, Tier 4 byte mutations)
  └── Aggregator (결과 통합, 중복 제거)
```

- 다수 단말에 동시 퍼징
- Worker별 독립 시드 공간
- 중앙 결과 집계 + 중복 finding 제거

### 5.3 크로스 플랫폼 단말 지원

| 플랫폼 | 관측 방법 | 난이도 |
|--------|----------|:------:|
| Android | ADB logcat + bugreport | 중간 |
| iOS | 제한적 (sysdiagnose, CrashReporter) | 높음 |
| 임베디드 IoT | UART/시리얼 로그 | 높음 |
| 자동차 IVI | CAN + 시리얼 | 매우 높음 |

### 5.4 퍼징 결과 분석 대시보드

```
┌─────────────────────────────────────────────┐
│             Fuzzing Dashboard               │
├─────────────┬───────────────┬───────────────┤
│ Campaign    │  Finding      │  Coverage     │
│ Progress    │  Timeline     │  Heatmap      │
│ (실시간)     │  (크래시 발견) │  (메서드별)    │
├─────────────┴───────────────┴───────────────┤
│ Method × Strategy × Layer Matrix            │
│ (어떤 조합이 가장 많은 이상을 발견했는가)       │
├─────────────────────────────────────────────┤
│ Top Findings (재현 명령어 포함)               │
└─────────────────────────────────────────────┘
```

### 5.5 CI/CD 통합 (Regression Fuzzing)

```yaml
# .github/workflows/fuzz-regression.yml
on:
  schedule:
    - cron: '0 2 * * *'  # 매일 새벽 2시

jobs:
  fuzz:
    runs-on: self-hosted  # UE 연결된 러너
    steps:
      - run: uv run fuzzer campaign run --scope tier1 --max-cases 500
      - run: uv run fuzzer campaign report --fail-on crash,anr,reboot
```

---

## 6. 실행 우선순위 요약

### 완료 ✅

```
┌─ 완료 ────────────────────────────────────────┐
│                                                │
│  1. OracleVerdict 데이터 모델 구현         ✅    │
│  2. SocketOracle 구현                    ✅    │
│  3. ProcessOracle 구현 (pgrep)           ✅    │
│  4. LogOracle 구현 (incremental read)    ✅    │
│  5. OracleEngine 3축 조합 판정           ✅    │
│  6. CampaignConfig + CaseGenerator      ✅    │
│  7. CampaignExecutor 기본 루프           ✅    │
│  8. ResultStore (JSONL)                 ✅    │
│  9. CampaignReporter + ReplayEngine     ✅    │
│                                                │
└────────────────────────────────────────────────┘
```

### 다음 (단기)

```
┌─ P0: 다음 ────────────────────────────────────┐
│                                                │
│  1. ISSUE-01 결정 (초기 타깃 단말 선정)           │
│  2. Stateful dialog fuzzing                    │
│  3. 소프트폰 대상 Tier 1 실제 캠페인 실행         │
│  4. real-ue-pcscf 경로 구현                    │
│  5. ADB logcat Observer 구현                   │
│  6. Android 실 단말 Tier 1 캠페인               │
│                                                │
└────────────────────────────────────────────────┘
```

### 확장 (장기)

```
┌─ P2: 확장 ─────────────────────────────────────┐
│                                                │
│  7. IMS AKA 인증 시뮬레이션                      │
│  8. TLS/WebSocket 전송                          │
│  9. 병렬/분산 퍼징                               │
│  10. Grammar-aware 뮤테이션                     │
│  11. 결과 분석 대시보드                           │
│  12. CI/CD regression fuzzing                  │
│  13. 크로스 플랫폼 확장 (iOS, IoT)               │
│                                                │
└────────────────────────────────────────────────┘
```

---

## 7. 마일스톤 정의

### M1: Socket-Only 캠페인 (소프트폰) ✅

> [!check] 완료
> - SocketOracle + ProcessOracle + LogOracle 3축 판정 동작
> - CampaignExecutor가 Tier 1~4 케이스를 순차 실행
> - 결과가 JSONL에 기록되고 `campaign report`/`campaign replay`로 조회·재현 가능
> - 270개 테스트 통과

### M2: ADB-Integrated 캠페인 (Android 실 단말)

> [!check] 완료 조건
> - ADB Observer가 crash/ANR/reboot를 실시간 감지
> - OracleAggregator가 socket + ADB 결과를 종합 판정
> - Android 단말 대상 Tier 1~2 캠페인 실행
> - 발견된 이상 케이스에 대한 재현 명령어 자동 생성

### M3: IMS 통합 캠페인

> [!check] 완료 조건
> - real-ue-pcscf 경로로 Tier 3 IMS private surface 실험 가능
> - P-Asserted-Identity, P-Charging-*, Feature-Caps 뮤테이션 캠페인 실행
> - 소프트폰 + 실 단말 + P-CSCF 경유 3가지 경로 모두 캠페인 지원

### M4: 자동화 + 분석

> [!check] 완료 조건
> - 다수 단말 병렬 퍼징
> - 결과 대시보드에서 method × strategy × layer 매트릭스 시각화
> - Regression fuzzing이 CI에서 자동 실행
> - Grammar-aware 뮤테이션이 기존 3계층에 통합

---

## 8. 기술 부채 및 개선 사항

현재 코드베이스에서 로드맵 진행 시 함께 해결해야 할 항목.

| 영역 | 내용 | 관련 Phase |
|------|------|-----------|
| **MSISDN 해석 경직성** | Docker 컨테이너 이름 하드코딩 (`VMF_REAL_UE_SCSCF_CONTAINER`) | 단기 |
| **비동기 미지원** | 동기 소켓 I/O만 사용, 다수 대상 병렬 전송 시 병목 | 장기 |
| **Observer 구조** | OracleEngine으로 3축 구현 완료, ADB/pcap 플러그인은 미구현 | 단기 |
| **에러 핸들링** | Sender에서 네트워크 오류 시 graceful recovery 부족 | 단기 |
| **설정 관리** | 환경변수 기반 — 캠페인 설정은 파일 기반이 더 적절 | 단기 |
| **로깅** | 구조화된 로깅 미도입 (structlog 등) | 단기 |

---

## 9. 관련 문서

- [[구현-문서]] — 구현 상세 (아키텍처, 모듈별 설계)
- [[프로젝트-개요]] — 프로젝트 전체 구조
- [[SIP-프로토콜-연구-종합]] — SIP 프로토콜 연구 결과
- [[SIP-공격면-우선순위표]] — Tier 1~4 공격면
- [[SIP-통합-연구-보고서]] — 통합 분석 및 권고
- [[PRD]] — 제품 요구사항
- [[PHASE4_PRD]] — Sender/Reactor 설계
- [[REAL_UE_DIRECT_PRD]] — 실 UE 직접 전송 설계
